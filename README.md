# FTT-App-Installation-and-Optimizer
Lightweight PowerShell + WinForms GUI to set up Windows fast. Pick from 50+ apps (installed via winget) and common debloat tweaks (disable telemetry, Copilot, Game Bar, startup bloat, etc.), then generate a single self-elevating, fault-tolerant .bat script. Clean UI, no bloat, no Electron
